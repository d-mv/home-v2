import styled from 'styled-components';
import { title } from '../tokens/typography';

const Title = styled.section`
	font: ${title};
	padding: 0.5rem 1rem 0.1rem 1rem;
	letter-spacing: 0.1rem;
`;

export default Title;
