import styled from 'styled-components';
import { location } from '../tokens/typography';

const Location = styled.section`
	font: ${location};
	padding: 0.1rem 1rem 0.5rem 1rem;
`;

export default Location;
